info:
  name: "Descargar Acuce "
  type: http
  seq: 8

http:
  method: GET
  url: "{{SERVER}}/acuse/pdf/issued/xrGIPDBXtk7yKr4QTXzOaA2"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
