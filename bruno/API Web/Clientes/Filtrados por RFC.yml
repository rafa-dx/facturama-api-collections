info:
  name: Filtrados por RFC
  type: http
  seq: 8

http:
  method: GET
  url: "{{SERVER}}/Client?keyword=URE180429TM6"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  params:
    - name: keyword
      value: URE180429TM6
      type: query
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
