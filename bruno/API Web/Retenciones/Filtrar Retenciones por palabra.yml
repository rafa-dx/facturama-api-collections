info:
  name: Filtrar Retenciones por palabra
  type: http
  seq: 4

http:
  method: GET
  url: "{{SERVER}}/Retenciones?keyword=CACX7605101P8&status=all"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  params:
    - name: keyword
      value: CACX7605101P8
      type: query
    - name: status
      value: all
      type: query
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
