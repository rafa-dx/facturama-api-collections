info:
  name: Detalle Retencion
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER}}/retenciones/j9HQOO7tP7WXlGN2vMzJZA2"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
