info:
  name: tarifffractions
  type: http
  seq: 19

http:
  method: GET
  url: "{{SERVER}}/catalogs/tarifffractions?keyworw="
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  params:
    - name: keyworw
      value: ""
      type: query
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Obtener los códigos de productos y servicios
