info:
  name: Regimen por tipo de RFC
  type: http
  seq: 18

http:
  method: GET
  url: "{{SERVER}}/Catalogs/FiscalRegimens?rfc=EKU9003173C9"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  params:
    - name: rfc
      value: EKU9003173C9
      type: query
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
