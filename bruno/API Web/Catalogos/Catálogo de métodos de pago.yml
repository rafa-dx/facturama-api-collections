info:
  name: Catálogo de métodos de pago
  type: http
  seq: 15

http:
  method: GET
  url: "{{SERVER}}/catalogs/PaymentMethods"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
