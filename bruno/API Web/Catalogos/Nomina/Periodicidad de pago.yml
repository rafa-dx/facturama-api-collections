info:
  name: Periodicidad de pago
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER}}/catalogs/paymentfrequencies"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Obtener  los códigos postales
