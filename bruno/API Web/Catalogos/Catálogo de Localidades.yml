info:
  name: Catálogo de Localidades
  type: http
  seq: 6

http:
  method: GET
  url: "{{SERVER}}/catalogs/localities?stateCode=MEX"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  params:
    - name: stateCode
      value: MEX
      type: query
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Listado de paises válidos
