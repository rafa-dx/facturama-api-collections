info:
  name: CartaPorte-ClaveUnidadPeso
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER}}/api/catalogs/cartaporte/ClaveUnidadPeso"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
