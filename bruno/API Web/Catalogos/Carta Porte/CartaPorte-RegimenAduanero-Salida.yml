info:
  name: CartaPorte-RegimenAduanero/Salida
  type: http
  seq: 13

http:
  method: GET
  url: "{{SERVER}}/api/catalogs/cartaporte/RegimenAduanero/Salida"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
