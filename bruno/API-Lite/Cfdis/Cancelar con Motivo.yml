info:
  name: Cancelar con Motivo
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{SERVER}}/api-lite/cfdis/Ar3WalEzVRz4LZ5oWPfXmw2?motive=03"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  params:
    - name: motive
      value: "03"
      type: query
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
