info:
  name: Obtener acuse de cancelación
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER}}/acuse/pdf/issuedLite/0L864p3ksJ3MjWf3or242g2"
  headers:
    - name: User-Agent
      value: "{{UserName}}"
  auth:
    type: basic
    username: "{{UserName}}"
    password: "{{Password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
